#include <stdio.h>
int main(){
    int n,som=0,i;
    printf("saisir un nombre :");
    scanf("%d",&n);
    for ( i = 1; i <= n; i++)
    {
       som=som+i;
    }
    printf("printf la somme est %d:",som);
    








    return 0;
}